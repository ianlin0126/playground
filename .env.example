KID_BOT_TOKEN=
KID_NAME=
KID_TELEGRAM_ID=
ANTHROPIC_API_KEY=
GITHUB_TOKEN=
GITHUB_REPO=
# Optional — OpenAI key for the game-assets skill. When set, the skill
# generates sprite atlases via GPT Image 2.0 instead of asking a human
# to paste the prompt into an image-gen tool.
OPENAI_API_KEY=
# Optional — server port. Defaults to 3000 if unset.
PORT=
